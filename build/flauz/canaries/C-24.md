# Canary C-24 — Subagents + mid-run steering + tool-confirmation signals

- **Matrix row (wave1/c)**: C-24 "Subagents + mid-run steering +
  tool-confirmation signals" — AVAILABLE ⚠.
  ⚠ gate: entitlement/auth for the built-in Claude/Codex providers; Flauz
  agents register as AHP-style providers or participants instead (DL-3/DL-5) —
  the *signals* are platform surface, not provider surface.
- **Tree evidence (verified @ 9bf9ae764da)** — signal union at
  `src/vs/platform/agentHost/common/agent.ts:880-884`, incl.:
  - `IAgentSubagentStartedSignal` / `…Resumed…` / `…Completed…` (subagent lifecycle)
  - `IAgentSteeringConsumedSignal` (mid-run steering consumed)
  - `IAgentToolPendingConfirmationSignal` (`agent.ts:952`)
  - `IAgentSpawnChatEvent` (`agent.ts:711`) — the spawn primitive subagents ride.
- **Owner CI**: `.github/workflows/flauz-canaries.yml` → job
  `c24-subagents-steering`.

## Setup

1. Same build recipe as C-20/C-23.
2. Preconditions (glob-probed): `extensions/flauz-agent` present; a flauz
   provider registered (lane F). PROBE-ONLY mode until then.

## Steps (scripted boot + one steered session)

1. Boot with verbose logging (recipe as C-20 §Steps-1).
2. **[driver — WAITING-ON-LANE F]** Start a session with the flauz participant
   whose plan includes a subagent (e.g. "verify with a reviewer subagent").
3. **[driver — WAITING-ON-LANE F]** While the run is in flight, inject a
   steering message ("prefer option B").
4. **[driver — WAITING-ON-LANE F]** Let a tool call that requires confirmation
   reach the pending state and approve it (HumanApproval gate — Wave-3 golden
   path, MIGRATION-PLAN §3 exit (a)).
5. Capture the AHP event/signal log for the whole run.

## Expected (assertions)

| # | Assertion | Mechanism | Status |
|---|---|---|---|
| A1 | AHP initializes cleanly — no `agentHost … crash|failed to start` | `grep` of boot log | boot-level, automated |
| A2 | Subagent lifecycle observed: Started → (Resumed)* → Completed signals for the spawned subagent | driver parses signal stream (agent.ts:880-884 union) | **WAITING-ON-LANE F** |
| A3 | Steering consumed mid-run: `IAgentSteeringConsumedSignal` after step 3, run continues (not aborted) | driver signal stream | **WAITING-ON-LANE F** |
| A4 | Tool confirmation pending state visible: `IAgentToolPendingConfirmationSignal` then approved → tool executes | driver signal stream + chat progress parts | **WAITING-ON-LANE F** |
| A5 | Signal shapes match the d.ts contracts at this HEAD (sync-drift guard) | the driver compiles against `src/vs/platform/agentHost/common/agent.ts` — any upstream rename breaks the build, which is the trip-wire | automated once driver lands |

## Drift trip-wires

- Upstream renaming/moving the signal union (`agent.ts:880-884`) breaks the
  driver build at the next sync → sync runbook blocks and re-points the driver
  (this is precisely the churn class DL-4 tracks for proposed APIs; AHP signals
  are core, not proposed, so the canary is the tracking surface).
- A2-A4 semantically regressing while shapes stay valid: escalate with the
  captured signal log to the TL (could be an AHP behavior change, not drift).
