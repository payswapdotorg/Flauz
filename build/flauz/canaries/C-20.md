# Canary C-20 — Default-agent re-point (model picker + model management UI)

- **Matrix row (wave1/c)**: C-20 "Model picker + model management UI" — AVAILABLE ⚠.
  ⚠ gate: the whole picker/management stack is gated on a *default chat agent*
  (`product.json:90-157` → Copilot). Flauz re-points `defaultChatAgent` via
  `product.flauz.json` (build-time overlay — DL-3/DL-12), never by patching
  `product.json`.
- **Tree evidence (all paths verified @ 9bf9ae764da)**:
  - `product.json:90` — `"defaultChatAgent"` block (Copilot wiring, entitlement URLs).
  - `src/vs/workbench/contrib/chat/browser/widget/input/modelPicker/` — the picker
    stack (`modelPickerActionItem.ts`, `modelPickerCard.ts`, …).
  - `src/vs/workbench/contrib/chat/browser/chatManagement/` — model management.
  - `src/vs/workbench/contrib/chat/browser/aiCustomization/` — management editor.
- **Owner CI**: `.github/workflows/flauz-canaries.yml` → job `c20-default-agent`
  (runs at EVERY sync — DL-11; also on flauz-path pushes).
- **Cross-ref**: Worker F's `build/flauz/canaries/default-agent-checklist.md`
  (lands with `extensions/flauz-agent` + `product.flauz.json`) — when it merges,
  this spec's WAITING-ON-LANE sections execute from that checklist; until then
  the boot-level assertions below are the canary.

## Setup

1. Build the flauz tree on an `ubuntu-latest` runner (upstream-pipeline steps:
   `node build/npm/preinstall.ts`, `npm install`, setup-electron composite
   action, `npm run compile`), start Xvfb (`build/azure-pipelines/linux/xvfb.init`,
   DISPLAY :10).
2. Precondition: `product.flauz.json` exists and re-points
   `defaultChatAgent.extensionId` to the flauz participant
   (e.g. `flauz.agent-bridge`) — **glob-probe, never assume**: the job skips the
   participant assertions with a recorded reason while lane F is in flight.

## Steps (scripted boot)

1. Boot: `DISPLAY=:10 ./scripts/code.sh --verbose --user-data-dir <fresh> --extensions-dir <fresh> --disable-telemetry --skip-welcome --skip-release-notes --disable-updates --disable-experiments --disable-workspace-trust > boot.log 2>&1 &`
2. Wait 25 s (startup + `LifecyclePhase.Eventually` idle window).
3. Capture `ps -eo pid,ppid,rss,pcpu,args` and the boot log.

## Expected (assertions)

| # | Assertion | Mechanism | Status |
|---|---|---|---|
| A1 | Clean boot, no crash/unresponsive | `! grep -qE 'Unresponsive|Fatal error' boot.log` | boot-level, automated |
| A2 | Affinity pin took (PERF §7 R1): `Placing extension(s) flauz.agent-bridge on a separate extension host.` | `grep` of boot log — log line emitted at `src/vs/workbench/services/extensions/common/extensionRunningLocationTracker.ts:222` | boot-level, automated |
| A3 | No proposed-API silently dropped (DL-4): the string `DOES NOT EXIST` must NOT appear | `grep` of boot log — warn emitted at `src/vs/workbench/services/extensions/common/extensionsProposedApi.ts:47` | boot-level, automated |
| A4 | Participant registered: `code/flauz/didRegisterParticipants` mark present | perf-mark log/telemetry (marks ride `$setPerformanceMarks` — PERF §6.2 item 1); WARN if the lane emits it via `TelemetryLogger` instead | boot-level, automated (tolerant) |
| A5 | Model picker opens listing the flauz participant's models | smoke-driver UI step (lane F golden-path suite; see `default-agent-checklist.md`) | **WAITING-ON-LANE F** |
| A6 | Model management editor (`chatManagement/`, `aiCustomization/`) lights up with the flauz participant as the managed default | smoke-driver UI step | **WAITING-ON-LANE F** |

## U-1 closure note

This canary is the mechanical half of U-1 ("which of the ⚠ surfaces light up
with a non-Copilot default agent"): A1-A4 prove the participant is the default
*agent* path with zero Copilot entitlement coupling; A5/A6 record which picker/
management surfaces light up for it — the results feed the U-1 closure spike in
the sync canary report attached to each `flauz/sync/<date>` tag.

## Drift trip-wires

If A2 disappears across a sync: upstream changed the affinity log line or the
`extensions.experimental.affinity` semantics → escalate to PERF §7 R1 (pinning
is perf-only degradation, not correctness — canary fails LOUD so the sync
runbook records it).
If A3 fires: the rota (job 5) already failed with the offending proposal name;
C-20 corroborates at boot level.
